# Input-route kit bootstrap

An optional iron input route was proposed while satisfying automation science through iron gears and plates. Acquiring its burner inserter required another gear; carrying the outer science/gear dependency path into that separate construction kit falsely reported a production cycle.

The route-kit acquisition starts a fresh dependency path while retaining the existing route-expansion and optional-capital guards. It still uses the output-buffer-aware planner, so plate pickups target the owned output chest instead of bypassing automated furnace ownership. Nested calls restore the prior guard value. The shared expansion budget and genuine recipe-cycle checks remain active.

The exact reported cycle was reproduced in a synthetic planner regression before the fix. The fixed planner returns an ordinary paid output-chest extraction; it does not place infrastructure, clear pending work or alter inventories during planning. Additional tests preserve genuine cyclic recipe rejection, guard restoration and expansion-budget limits. Focused input-route/output-buffer tests: 92 passed.

Full offline validation: 2355 passed, 77 skipped; browser coverage is checked separately in hosted CI. No native performance claim is made.
